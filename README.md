<<<<<<< HEAD
# misiSSR проект

## Описание
NFTProject - это проект, который использует технологию блокчейн для создания и управления NFT (невзаимозаменяемыми токенами). Проект позволяет пользователям выпускать и управлять цифровыми сертификатами, которые могут использоваться в различных областях, включая трудоустройство и обучение, так же есть возможность подписывать сертификаты цифровой подписью с помощью VC, проект можно использовать как со стороны работодателя так и со стороны сотрудника, в рамках проекта конечно было со стороны сотрудника, но просим прощения за форс-мажор 

## Установка

### Предварительные требования
Убедитесь, что у вас установлены следующие инструменты:
- [Node.js](https://nodejs.org/) (версия 14 или выше)
- [Git](https://git-scm.com/)
- работает как на IDE WebStorm так и на VS Code, я все проверил заранее так что приятного тестирования)))







### Запуск проекта
## Шаг 1: Очистка и компиляция

Перед запуском проекта вам нужно очистить предыдущие сборки и скомпилировать ваши контракты. Выполните следующие команды:

npx hardhat clean

npx hardhat compile

## Шаг 2: Запуск локального узла

Запустите локальный узел Ethereum:

npx hardhat node

Эта команда запускает локальный блокчейн, который даст нам кошельки которые мы в дальнейшем будем использовать для демонстрации тестирования. Обратите внимание на адреса и порты, которые будут отображены в консоли.

## Шаг 3: Развертывание контракта

В новом терминальном окне выполните команду для развертывания моего смарт-контракта на локальном узле:

npx hardhat run scripts/deploy.js --network localhost



## Шаг 4: Запуск сервера

Теперь запустите сервер, который будет взаимодействовать с моим смарт-контрактом:

node server.js

Сервер будет запущен на http://localhost:3000.


### Команды для работы с NFT

Создание NFT: используйте соответствующий эндпоинт для создания нового NFT.

Отмена NFT: используйте соответствующий эндпоинт для отмены (сжигания) NFT.


### Подключение MetaMask к Hardhat Network

Убедитесь, что у вас установлен MetaMask:


Установите расширение MetaMask в вашем браузере, если вы этого еще не сделали. Вы можете найти его в официальном магазине Chrome или других браузерах.

Запустите локальный узел Hardhat:

Запустите ваш локальный узел Hardhat, используя команду:

npx hardhat node

После выполнения этой команды вы увидите адреса кошельков, сгенерированные Hardhat, в консоли. Эти адреса можно использовать для тестирования.

Откройте MetaMask:


Кликните на иконку MetaMask в вашем браузере, чтобы открыть расширение.

Добавьте новую сеть:

В MetaMask выберите выпадающее меню сети в верхней части окна (по умолчанию должно быть выбрано "Ethereum Mainnet") и нажмите на "Добавить сеть" или "Custom RPC" (в зависимости от версии MetaMask).

Заполните информацию о сети:

Введите следующую информацию в поля для новой сети:

Network Name: Hardhat Network

New RPC URL: http://localhost:8545

Chain ID: 31337

Currency Symbol (необязательно): ETH

Block Explorer URL (необязательно): оставить пустым

Нажмите на "Сохранить" или "Добавить сеть".

Подключите аккаунт:

После добавления сети выберите её в MetaMask, и вы сможете видеть ваш кошелек Hardhat Network. Копируйте адрес одного из кошельков, отображенных в консоли при запуске npx hardhat node, и импортируйте его в MetaMask, если это необходимо.

Используйте локальный узел:

Теперь вы можете использовать MetaMask для взаимодействия с вашим локальным узлом Hardhat. Например, вы можете использовать его для отправки транзакций, развертывания смарт-контрактов и проверки состояния вашего приложения.


### Теперь перейдем к сути функционала:

адрес овнера заранее уже добавлен в файл .env, я взял первый приватный ключ который мы сгенерировали, если вдруг захотите затестить в реальных условиях то просто вставьте приватный ключ своего кошелька, я уже тестил, 
но чтобы все работало надо покрывать комиссию, а значит у вас должен быть кошелек с ETH, а мы бедные студенты поэтому к сожалению обходимся локалкой))))
далее во всех полях вы вводите все данные которые нужны, все это пойдет в метаданные токена, а именно:

1)адрес кошелька на который отправляется токен

2)имя 

3)фамилия

4)организация где он работает

поясню: в рамках нашего кейса мы делаем сертификаты о трудоустройстве, которые в дальнейшем верефицируем, но так же у нас были идеи касаемо достижений, наград в виде нфт токенов, которые, при правильном написании контракта, обменивались бы на реальные деньги, то есть это было бы что-то вроде премирования в виде создания токенов на определенных условиях

5) картинка токена (на демо-видео видно как я добавляю токены), но к сожалению картинки метамаск подгружать не хочет, подозреваю что из-за локальной сети

6) далее мы переходим к подписи, а начинается с ввода названия организации

поясняю: организация которая подписывает, так как с фронтедом форс мажор вышел то проект становится универсальным если внедрить его в свою платформу)))(P.S. сам не ожидал с нулевыми знаниями сделаю что-то подобное)

7) дата


# Теперь можно нажимать кнопку подписать, в всплывающем окне мы видим хэш, который так же хранится в метаданных 

При создании токена, например, NFT, процесс подписания включает несколько этапов:

Генерация ключей: Создатель токена генерирует пару ключей: приватный и публичный. Приватный ключ используется для подписи, а публичный — для верификации.

Создание токена: Токен создается с определенными атрибутами (например, название, описание, метаданные). Эта информация формирует хэш.

Подпись: Хэш токена шифруется с использованием приватного ключа создателя, что создает цифровую подпись.

Сохранение информации: Токен, его метаданные и подпись сохраняются в блокчейне.

Верификация: Когда кто-то хочет проверить подлинность токена, он может использовать публичный ключ создателя для расшифровки подписи. Если расшифрованный хэш совпадает с хэшем токена, это подтверждает его подлинность.

# далее нажимаем на кнопку создать NFT


Мы увидим всплывающее окошко в котором написано что токен успешно создан, а в консоли на сайте можно даже посмотреть айди, а в консоли хардхэта мы увидим что функция сработала и увидим введенные метаданные, а в терминале на котором открывали сервер мы ничего не увидим 
так как ошибок нет :)

Что касаемо айди, отчет начинаетя с нуля поэтому советую нажать кнопку создать NFT 2 раза, чтобы айди токена был 1, а не 0

# далее кнопка проверить подлинность 

:( к сожалению она не работает так как локальная сеть просто крашится, времени не хватило разобраться что с ней не так,  так что будет справедливо не засчитывать, но я подозреваю что в реальных условиях все бы сработало, жаль денег затестить нет

# отзыв токена должен работать, в метамаске после интеграции токен будет отображаться, но взаимодействовать с ним не выйдет, это я уже затестил 


### Добавление токена в MetaMask


Шаг 1: Откройте MetaMask

Кликните на иконку MetaMask в вашем браузере, чтобы открыть расширение.

Шаг 2: Перейдите на вкладку "Assets" (Активы)

В верхней части окна MetaMask выберите вкладку "Assets" (Активы). Здесь вы увидите список всех токенов, которые уже добавлены в ваш кошелек.

Шаг 3: Добавьте новый токен


Нажмите на кнопку "Import tokens" (Импортировать токены) внизу страницы.

Выберите вкладку "Custom Token" (Пользовательский токен).


Введите адрес контракта токена:

Token Contract Address (Адрес контракта токена): Вставьте адрес контракта вашего токена (обычно предоставляется в документации проекта или на сайте).

MetaMask автоматически заполнит поля Token Symbol (Символ токена) и Token Decimal (Десятичные знаки). Если нет, введите их вручную (обычно это 18 десятичных знаков для большинства токенов).

Нажмите на кнопку "Add Custom Token" (Добавить пользовательский токен), затем подтвердите, нажав "Import Tokens" (Импортировать токены).

Посмотреть то что токен выпущен можно в терминале где открывался локальный узел Ethereum

После проверки работоспособности проекта в создании нфт выпуске и отзыве вам остается только интегрировать все в свою платформу


P.S. Создатель проекта я очень старался   :))
=======
# NFTProject

